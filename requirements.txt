# Core dependencies
streamlit==1.45.1
openai>=1.0.0
numpy<2.0.0  # NumPy 2.0과의 호환성 문제 방지

# Vector search
faiss-cpu==1.11.0

# NLP models
sentence-transformers==4.1.0
transformers<5.0.0  # 버전 제한으로 안정성 확보

# 토치 관련 (버전을 명시적으로 지정)
torch==2.1.0  # 2.7.1 대신 더 안정적인 버전 사용
--extra-index-url https://download.pytorch.org/whl/cpu  # CPU 버전만 설치

# 기타 필수 라이브러리
pandas<2.0.0  # 호환성을 위해 1.x 버전 사용
scikit-learn

# 선택적 라이브러리 (원본 코드를 사용하려면)
# cryptography>=41.0.0
# ijson>=3.2.0
